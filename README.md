# flutter\_app_template

## 简介
flutter项目模板，一键创建空项目，包含登录页、Tab页、页面导航、用户状态管理。

自动引入以下依赖库：

```
dio: ^2.1.13
shared_preferences: 0.5.3+4
provider: ^3.1.0
```

避免每次创建flutter项目时进行一些重复的工作。

## 用法
终端执行以下指令

```
$ cd {flutter_app_template所在目录}
$ ./flutter_app_template/flutter_app.sh ./myapp
```
